package Misc;

public class Unused {

/* This file contains miscellaneous code that may be useful

 Changing cursor 
void ImageCursor() {
	Image beetleImage = new Image("file:img/harry.png");
	ImageCursor squash = new ImageCursor(beetleImage, 5,5);
	pane.setCursor(squash);
}


private void drawBackground(GraphicsContext gc) {

gc.setFill(Color.web("AAD751"));
gc.fillRect(0, 0, 500, 500);

for (int i = 0; i < rows; i++) {
    for (int j = 0; j < columns; j++) {
        if ((i + j) % 2 == 0) {
            gc.setFill(Color.web("AAD751"));
        } else {
            gc.setFill(Color.web("A2D149"));
        }
        gc.fillRect(i * tileSize, j * tileSize, tileSize, tileSize);
    }
}

*/
//}

}